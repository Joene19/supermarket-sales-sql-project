CREATE TABLE sales (
    order_no INT,
    order_date VARCHAR(20),
    customer_name VARCHAR(100),
    ship_date VARCHAR(20),
    retail_price DECIMAL(10,2),
    order_quantity INT,
    tax DECIMAL(10,2),
    total DECIMAL(10,2)
);